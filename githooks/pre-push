#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "[pre-push] Validating output tracking policy..."
Rscript scripts/check_latest_output_tracking.R "$repo_root"
