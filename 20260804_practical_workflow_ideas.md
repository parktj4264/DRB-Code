# Practical Workflow Ideas

작성일: 2026-08-04

## 목적

Spotfire에서 필요한 화면을 하나씩 캡처하고 보고용 PPT에 복사하는 시간을 줄인다. 코드는 반복 작업인 plot 생성과 PPT 배치를 담당하고, 엔지니어는 어떤 내용을 최종 보고에 포함할지 판단하는 데 집중한다.

## 권장 운영 구조

### 1. Main Stage PPT: 필수 체크 항목

- 사용자가 명시적으로 선택한 MSR만 수록한다.
- 공식 보고용 PPT를 완성할 때 기준이 되는 본체로 사용한다.
- 정해진 템플릿과 배치를 유지하여 매 실행 결과가 안정적으로 나오게 한다.
- 엔지니어는 이 PPT를 먼저 열고 필수 항목을 확인한다.

### 2. Suggested PPT: 이것도 봐보세요

- 사용자가 선택하지 않았지만 Sigma 기준을 초과한 항목을 자동으로 제안한다.
- Main Stage PPT 옆에 열어두는 후보 슬라이드 모음으로 사용한다.
- 실제로 의미 있다고 판단된 슬라이드만 Main Stage PPT 또는 최종 보고용 PPT로 끌어와 추가한다.
- 필수 항목과 중복되는 MSR은 제외하고, 각 슬라이드에 추천 이유와 Sigma 값을 명확히 표시한다.
- 추천 항목이 너무 많아지지 않도록 우선순위가 높은 항목부터 제한적으로 보여준다.

### 3. Spotfire: 추가 탐색 및 안전망

- 코드와 Sigma가 포착하지 못한 이상이나 주변 패턴을 엔지니어가 직접 살펴본다.
- `results.csv`를 MSR 선택 및 마킹용 control table로 사용한다.
- 선택한 MSR에 맞추어 raw scatter, CDF, WFMAP, 구배/Wordline 등 관련 시각화를 연결한다.
- 색상, 조건, 그룹 및 추가 plot을 자유롭게 바꾸어 원인을 확인한다.
- Spotfire에서 새롭게 발견한 내용도 필요하면 캡처하거나 기존 자동 생성 slide와 함께 최종 보고에 추가한다.

## 실제 사용 시나리오

1. `run.R`을 실행하여 분석 결과, Main Stage PPT, Suggested PPT 및 Spotfire용 데이터를 갱신한다.
2. Main Stage PPT에서 사용자가 지정한 필수 MSR을 먼저 검토한다.
3. Suggested PPT를 옆에 열고 자동 추천된 Sigma 이상 항목을 빠르게 훑어본다.
4. 보고 가치가 있는 추천 slide만 Main Stage PPT 또는 별도의 최종 보고용 PPT로 끌어온다.
5. 판단 근거가 부족하거나 두 PPT에서 보이지 않는 패턴은 자동으로 열린 Spotfire에서 추가 확인한다.
6. 최종 PPT에는 실제 의사결정에 필요한 slide만 남긴다.

## 이 구조의 장점

- 반복적인 Spotfire 캡처·복사 시간을 크게 줄일 수 있다.
- 자동 추천을 공식 보고서에 무조건 포함하지 않고 사람이 마지막으로 판단한다.
- Main Stage PPT가 자동 추천 항목으로 난잡해지는 것을 막는다.
- Sigma가 놓칠 수 있는 패턴은 Spotfire 탐색으로 보완한다.
- 자동 생성 slide의 규격이 일정하므로 최종 보고용 PPT를 빠르게 조립할 수 있다.

## 향후 구현 시 고려사항

- Main Stage와 Suggested PPT는 같은 템플릿과 slide 크기를 사용하여 서로 자연스럽게 복사되도록 한다.
- Suggested PPT에는 `자동 추천`, 추천 기준, `|Sigma|`, 비교 그룹을 표시한다.
- 필수 선택 항목과 자동 추천 항목의 중복을 제거한다.
- 표본 수 부족, 결측 과다 등 신뢰하기 어려운 결과는 자동 추천에서 제외하거나 경고를 표시한다.
- Sigma 초과 항목이 없으면 억지로 추천하지 않고 `추가 추천 항목 없음`으로 명확히 표시한다.
- 초기에는 추천 개수를 작게 유지하고, 실무 피드백을 받은 뒤 필요할 때만 늘린다.

## `run.R` 파라미터 방향 메모

현재처럼 하나의 파라미터로 `필수 체크 항목만 넣기`와 `필수+자동 추천 항목을 모두 넣기`를 선택하고, 그 결과를 Summary와 상세 slide에 동시에 적용하는 방식은 출력물이 분리되면 재검토가 필요하다.

향후에는 다음 역할을 서로 독립적으로 다루는 방향을 검토한다.

- Main Stage PPT에는 사용자가 선택한 필수 체크 항목을 넣는다.
- Suggested PPT에는 Sigma 기준으로 자동 추천된 항목을 별도로 넣는다.
- 자동 추천 PPT의 생성 여부, 추천 기준 및 최대 추천 개수는 별도 옵션으로 둔다.
- Summary에 자동 추천 항목을 함께 표시할지는 상세 slide 생성 범위와 분리하여 선택할 수 있게 한다.
- 하나의 `필수만/둘 다` 옵션이 Summary 구성과 PPT 구성을 동시에 결정하지 않도록 역할을 나눈다.

아직 파라미터 이름이나 기본값은 확정하지 않는다. 실제 업무에서 Main Stage PPT, Suggested PPT 및 Summary를 함께 사용해 본 뒤 가장 단순한 입력 방식으로 결정한다.

## PPT별 Summary와 상세 slide 구성

상세 slide의 기본 방향은 유지한다.

- `run.R`에서 입력받는 카테고리 범위를 해당 실행의 분석 및 보고 대상 범위로 사용한다.
- Main Stage PPT의 Summary에는 사용자가 필수로 체크한 MSR만 표시한다.
- Main Stage PPT의 상세 slide도 사용자가 체크한 MSR만 생성한다.
- Main Stage에는 지정된 카테고리 범위 안에 있으면서 필수로 체크된 항목만 포함한다.
- 지정된 카테고리 범위 안에 있지만 체크되지 않은 항목은 Main Stage에 별도 문구나 빈 항목을 만들지 않고 그대로 비워 둔다.
- Suggested PPT의 Summary에는 지정된 카테고리 범위 안에서 Sigma 기준을 초과한 MSR을 누락 없이 모두 정리한다.
- Sigma 초과 항목이 많으면 Summary 표를 여러 페이지로 나누어 전부 보여준다.
- Suggested Summary는 `|Sigma|`가 큰 순서 등 일정한 기준으로 정렬하여 취사선택하기 쉽게 만든다.
- Suggested Summary에는 필수 체크 여부와 Main Stage 포함 여부를 함께 표시하여 중복 여부를 바로 알 수 있게 한다.
- Suggested PPT의 상세 후보 slide는 Main Stage와 중복되지 않는 항목을 대상으로 한다.
- Suggested Summary는 전체 후보 목록, 상세 slide는 실제로 가져다 쓰기 좋은 후보 자료라는 역할로 구분한다.

## 필수 체크 상태별 처리 원칙

### 필수 체크가 없는 경우

- Sigma 초과 항목을 Main Stage의 필수 항목으로 자동 승격하지 않는다.
- Main Stage에는 별도의 `선택된 필수 항목 없음` 문구를 쓰지 않고 체크되지 않은 영역을 그대로 비워 둔다.
- Suggested PPT는 정상적으로 생성하여 Sigma 초과 항목 전체를 Summary에 보여준다.
- 엔지니어는 Suggested PPT와 Spotfire를 검토한 뒤 필요한 slide를 최종 보고용 PPT에 선택하여 넣는다.

### 필수 체크가 일부만 있는 경우

- Main Stage PPT에는 체크된 항목만 정확히 넣고 자동으로 개수를 채우지 않는다.
- 체크되지 않은 Sigma 초과 항목은 Suggested PPT에서 확인한다.
- Sigma 초과 항목 중 이미 Main Stage에 포함된 항목은 Suggested Summary에는 표시하되 `Main 포함` 상태를 붙인다.
- 이미 Main Stage에 상세 slide가 있는 항목은 Suggested 상세 slide에서 중복 생성하지 않는 방향을 우선 검토한다.

### 추가 예외 처리

- 체크한 MSR 이름이 실제 데이터에 없으면 조용히 무시하지 않고 누락 항목을 경고한다.
- Sigma 초과 항목이 없으면 Suggested Summary에 `추가 추천 항목 없음`을 표시한다.
- 표본 수 부족이나 결측 과다로 판단하기 어려운 항목은 일반 추천과 구분하여 표시하거나 추천 대상에서 제외한다.
- 카테고리 범위 밖의 항목은 Main 및 Suggested PPT 후보에서 제외하고 Spotfire에서 필요할 때 추가 탐색한다.

## Rev1 완료 기준 아이디어

다음 세 가지 흐름이 연결되면 실무 활용을 위한 Rev1이 완성된 것으로 본다.

1. 사용자가 체크한 항목으로 Main Stage PPT를 자동 생성한다.
2. Sigma 초과 항목 전체 Summary와 필요한 후보 slide를 Suggested PPT로 제공한다.
3. Spotfire용 데이터를 자동 갱신하고 DXP를 열어 두 PPT에서 부족한 내용을 즉시 탐색할 수 있게 한다.

## 한 줄 정의

Main Stage PPT는 반드시 볼 내용을 담는 보고의 본체, Suggested PPT는 가져다 쓸 후보 slide 보관함, Spotfire는 자동화가 놓친 내용을 찾는 탐색 작업대로 운영한다.

## 2026-08-05 추가 아이디어: 한 PPT 통합안

Main과 Suggested를 파일 두 개로 나누지 않고, 하나의 PPT 안에서 페이지 구역으로 명확히 구분하는 방식도 검토한다.

예상 구성은 다음과 같다.

1. `필수/Main Summary`: 카테고리별 대표 속성을 정리한다.
2. `필수 상세`: 사용자가 필수로 체크한 MSR의 상세 plot을 배치한다.
3. `Sigma 이상 Summary`: 기준을 초과한 MSR을 카테고리별로 전부 정리하고, 많으면 여러 페이지로 나눈다.
4. `Sigma 이상 상세`: 보고에 가져다 쓸 수 있도록 기준 초과 MSR의 상세 plot을 배치한다. 필수 항목과 겹치더라도 포함할 수 있다.

각 구역은 구분 페이지, 제목 접두어 또는 색상 등으로 명확하게 나누어 `반드시 볼 내용`과 `추가 검토 후보`가 섞여 보이지 않게 한다.

이 방식의 장점은 파일 하나만 열어도 전체 흐름을 순서대로 검토할 수 있고, 최종 보고용 PPT로 가져갈 slide를 한곳에서 고를 수 있다는 점이다. 반면 Sigma 초과 항목이 많으면 PPT가 길어지고 필수 영역과 추천 영역의 경계가 흐려질 수 있으므로 구역 구분이 핵심이다.

현재의 두 PPT 방식과 이 통합안을 실제 사용 관점에서 비교한 뒤 최종 방향을 결정한다. 이 내용은 아이디어 메모이며, 현 시점에서는 코드와 기존 출력 구조를 변경하지 않는다.
